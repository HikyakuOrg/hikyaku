"use client"

// TODO: not built yet. The previous version fetched a page of warehouse packages
// and discarded the result without rendering it; the dead fetch was removed rather
// than left making a request no one reads.
export function WarehousePackagesCard({ warehouseId: _warehouseId }: { warehouseId: string }) {
    return (
        <div></div>
    )
}
